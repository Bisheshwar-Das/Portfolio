<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="theme-color" content="#1e1e2f" />
  <title>
    <%= title || 'Bisheshwar Das - Portfolio' %>
  </title>
  <!--Bootstrap-->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <!-- AOS CSS -->
  <link href="https://unpkg.com/aos@2.3.4/dist/aos.css" rel="stylesheet">

  <style>
    body {
      background: linear-gradient(135deg, #1e1e2f 0%, #12121c 100%);
      color: #f0f0f0;
      font-family: 'Poppins', sans-serif;
    }
    .navbar-glass {
      background-color: rgba(30, 30, 47, 0.65);
      backdrop-filter: blur(10px);
      -webkit-backdrop-filter: blur(10px);
      border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    }
    .navbar {
      position: sticky;
      top: 0;
      z-index: 1030;
    }

    .parallax {
      background-image: url('/images/hero-bg.jpg');
      background-position: center;
      background-repeat: no-repeat;
      background-size: cover;
      background-attachment: fixed;
      position: relative;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      overflow: hidden;
    }

    @media (max-width: 768px) {
      .parallax {
        background-attachment: scroll;
      }
    }

    a,
    h1,
    h2,
    h3,
    h5,
    p {
      color: #f0f0f0;
    }

    section.container {
      background-color: rgba(255, 255, 255, 0.02);
      border-radius: 16px;
      padding: 2rem;
      margin-top: 1rem;
      margin-bottom: 1rem;
    }
    .glass {
      background: rgba(255, 255, 255, 0.03);
      backdrop-filter: blur(10px);
      -webkit-backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.1);
      color: #f0f0f0;
    }
    

    .card,
    .glass-card {
      background: rgba(255, 255, 255, 0.07);
      border-radius: 1rem;
      backdrop-filter: blur(16px) saturate(180%);
      -webkit-backdrop-filter: blur(16px) saturate(180%);
      border: 1px solid rgba(255, 255, 255, 0.2);
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
    }

    .card-title,
    .card-text {
      color: #f8f9fa;
    }

    .btn-warning {
      background-color: #ffae00;
      border: none;
      color: #1e1e2f;
    }

    .btn-warning:hover {
      background-color: #ffc940;
    }

    .btn-outline-primary {
      border-color: #00f2ff;
      color: #00f2ff;
    }

    .btn-outline-primary:hover {
      background-color: #00f2ff;
      color: #1e1e2f;
    }


    .glass-card:hover {
      transform: scale(1.05);
    }

    @keyframes parallaxZoom {
      0% {
        transform: scale(1);
      }

      100% {
        transform: scale(1.05);
      }
    }
    .footer-glass {
      background: rgba(30, 30, 47, 0.85);
      backdrop-filter: blur(12px);
      -webkit-backdrop-filter: blur(12px);
      color: #f0f0f0;
      border-top: 1px solid rgba(255, 255, 255, 0.1);
    }
    .footer-glass h5 {
      color: #ffffff;
    }
    .footer-glass a:hover {
      color: #00f2ff;
    }
      /* Override Bootstrap .text-muted for dark bg */
  .text-muted {
    color: #bbb !important;
  }
    
  </style>
</head>

<body>

  <nav class="navbar navbar-expand-lg navbar-dark navbar-glass">
    <div class="container">
      <a class="navbar-brand" href="/">Bisheshwar Das</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navmenu">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navmenu">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item"><a href="/" class="nav-link <%= active === 'home' ? 'active' : '' %>">Home</a></li>
          <li class="nav-item"><a href="/projects"
              class="nav-link <%= active === 'projects' ? 'active' : '' %>">Projects</a></li>
          <!-- <li class="nav-item"><a href="/skills" class="nav-link <%= active === 'skills' ? 'active' : '' %>">Skills</a>
          </li> -->
          <li class="nav-item"><a href="/contact"
              class="nav-link <%= active === 'contact' ? 'active' : '' %>">Contact</a></li>

          <% if(user) { %>
            <li class="nav-item"><a href="/auth/logout" class="nav-link">Logout</a></li>
            <li class="nav-item"><a href="/admin/dashboard" class="nav-link">Dashboard</a></li>
            <% } else { %>
              <li class="nav-item"><a href="/auth/login"
                  class="nav-link <%= active === 'login' ? 'active' : '' %>">Login</a></li>
              <% } %>
        </ul>
      </div>
    </div>
  </nav>